# keironTest

# DB (MySQL)
Steps:
1) Copy the content of scriptDB.txt file to create database and user with privileges

# Server (NodeJS with Express)
Steps:
1) cd server
2) npm i
3) npm run dev

# Client (React with AntDesign)
Steps:
1) cd client
2) yarn install
3) yarn dev
